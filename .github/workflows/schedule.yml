name: Schedule

on:
  schedule:
    - cron: "*/10 * * * *"

jobs:
  run:
    runs-on: ubuntu-latest

    steps:
      - name: Run
        run: node ${{ github.action_path }}/dist/index.js
        shell: bash
